<% if current_user != follow_user %>
  <div id="follow_form">
  <% if current_user.following?(follow_user) %>
    <%= render 'unfollow', follow_user: follow_user %>
  <% else %>
    <%= render 'follow', follow_user: follow_user %>
  <% end %>
  </div>
<% end %>